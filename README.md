# resume-auto

Tampermonkey 用户脚本：BOSS直聘筛选批量投递 + 自定义招呼语 + 浮层终端。

## 安装
1. 安装[篡改猴](https://www.tampermonkey.net/)
2. `npm install && npm run build`
3. 将 `dist/resume-auto.user.js` 导入篡改猴

## 使用

### 推荐流程（站内粗筛 + 脚本精筛）

脚本**不会**替你点 BOSS 顶部的城市/行业等下拉框，也**不会**自己翻页抓全站。它只处理**当前职位列表页上已渲染的 DOM**：读卡片文字 → 规则匹配 → 模拟点击「立即沟通」。

因此推荐两层筛选：

```text
① 你在 BOSS「职位」列表页做站内粗筛
        ↓
② 脚本配置里做第二层精筛（关键词 / 排除公司 / 薪资）
        ↓
③ 点「开始」→ 扫描 → 筛选 → 去重 → 配额 → 投递（可选招呼语）
```

**步骤：**

1. 登录 BOSS直聘，打开左侧为职位列表的页面（「职位」栏目）
2. **先用站内筛选**收窄：城市、求职类型、薪资、经验、学历、行业等（列表越短越好）
3. 右下角 `resume-auto` 浮层 →「**配置**」
   - 职位名包含：如 `嵌入式,硬件,单片机`
   - 公司名排除：如 `外包,外派,人力`
   - 薪资上下限、日配额（试跑建议先 `5`）、招呼语（可空）
4. 「**保存配置**」→「**开始**」
5. 盯日志：`扫描 N 条` → `筛选通过` → 每条 `ok / skip / fail`
6. 需要暂停/停止随时点；可「导出」日志核对

**注意：**

- 只处理**当前已加载**的列表；要投更多，自己滚动/翻页后再点开始
- 已投过的会去重跳过；当日配额满会停
- 折叠成小球后：可拖动位置；球上短码表示状态（`RUN` 投递中 / `PAU` 暂停 / `END` 结束等）；点球展开
- 选择器失效（扫描为 0）时，用 DevTools 核对后改 `src/platforms/boss/selectors.js` 再 build

### 配置字段与「不填」时的行为

### 配置字段与「不填」时的行为

配置保存在浏览器本地（篡改猴 GM 存储），刷新后仍在。

| 字段 | 默认 | 留空 / 默认时会发生什么 |
|------|------|------------------------|
| **职位名包含** | 空 | **不过滤职位名**，列表里扫到的岗位都会进入后续筛选（仍会受公司/薪资/去重限制） |
| **公司名包含** | 空 | **不限定公司**，任何公司都可通过 |
| **公司名排除** | 空 | **不排除任何公司**；填「外包,人力」等逗号分隔后，公司名含这些词的会跳过 |
| **描述排除词** | 空 | 不按描述过滤。**注意：当前版本 extract 尚未抓取职位描述**，该字段暂不生效（界面可配，属预留） |
| **薪资下限 (K)** | `0` | 表示**不设下限**。仅当上下限都为 0 时完全忽略薪资；只填下限时上限视为不限 |
| **薪资上限 (K)** | `0` | 表示**不设上限**。薪资区间与期望有交集才通过（如 `8-12K` 对 15–40 会被排除） |
| **日配额** | `100` | 当日最多成功投递 100 条；达到后整批停止并提示。可改小（建议试跑先设 5–10） |
| **招呼语** | 空 | **只投递、不发送自定义招呼语**（仍会走 BOSS 默认「立即沟通」流程）。填写后，投递成功再尝试在会话里发送该文案 |
| **无薪资跳过** | 未勾选 | 面议/无薪资文案的岗位**仍会投**。勾选后，解析不出薪资区间且未配薪资条件时会跳过 |

### 推荐最小配置（试跑）

1. **职位名包含**：你的方向，如 `前端,React,Vue`
2. **公司名排除**：如 `外包,外派,人力`
3. **日配额**：先 `5`
4. **招呼语**：一句简短自我介绍（可先空着只测投递）
5. 点「开始」，盯日志：扫描数 → 筛选通过 → 每条 `ok/skip/fail`

### 未配置时点「开始」会怎样？

- 会**对当前列表页扫到的全部岗位**（去重、未超配额）尝试投递  
- 没有关键词限制时可能投得过宽，**务必先缩小站内筛选或填「职位名包含」**  
- 已投过的会跳过；当日配额满 100 会停

## 开发
- `npm test` — core 单测
- `npm run dev` — 终端 UI 预览（index.html）
- 选择器：`src/platforms/boss/selectors.js`

## 免责
仅供个人求职学习交流，使用自动化有账号风控风险，禁止商用。
